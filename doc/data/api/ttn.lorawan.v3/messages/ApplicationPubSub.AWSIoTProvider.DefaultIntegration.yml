name: ApplicationPubSub.AWSIoTProvider.DefaultIntegration
fields:
- name: stack_name
  comment: The stack name that is associated with the CloudFormation deployment of
    The Things Enterprise Stack integration.
  type: string
  rules:
    max_len: 128
    pattern: ^[A-Za-z][A-Za-z0-9\-]*$
  default: ""
