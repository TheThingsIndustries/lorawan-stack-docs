name: MHDR
fields:
- name: m_type
  enum:
    name: MType
  rules:
    defined_only: true
  default: JOIN_REQUEST
- name: major
  enum:
    name: Major
  rules:
    defined_only: true
  default: LORAWAN_R1
