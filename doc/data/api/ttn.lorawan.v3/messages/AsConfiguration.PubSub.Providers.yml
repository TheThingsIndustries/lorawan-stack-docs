name: AsConfiguration.PubSub.Providers
fields:
- name: mqtt
  enum:
    name: AsConfiguration.PubSub.Providers.Status
  default: ENABLED
- name: nats
  enum:
    name: AsConfiguration.PubSub.Providers.Status
  default: ENABLED
