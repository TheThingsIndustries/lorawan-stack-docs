name: IsConfiguration.AdminRestrictions
fields:
- name: managed_fields_updates
  comment: |-
    managed-fields-updates disables the ability of an admin user to update fields which are administered by the
    billing providers. The restrictions are ignored if a tenant admin key is used.
  message:
    package: google.protobuf
    name: BoolValue
  default: null
