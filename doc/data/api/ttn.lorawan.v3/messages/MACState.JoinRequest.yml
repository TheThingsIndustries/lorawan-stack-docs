name: MACState.JoinRequest
fields:
- name: downlink_settings
  message:
    name: DLSettings
  rules:
    required: true
  default: {}
- name: rx_delay
  enum:
    name: RxDelay
  rules:
    defined_only: true
  default: RX_DELAY_0
- name: cf_list
  message:
    name: CFList
  default: {}
