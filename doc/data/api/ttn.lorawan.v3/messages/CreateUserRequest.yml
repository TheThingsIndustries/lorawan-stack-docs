name: CreateUserRequest
fields:
- name: user
  message:
    name: User
  rules:
    required: true
  default: {}
- name: invitation_token
  comment: The invitation token that was sent to the user (some networks require an
    invitation in order to register new users).
  type: string
  default: ""
