name: PacketBrokerInfo
fields:
- name: registration
  comment: The current registration, unset if there isn't a registration.
  message:
    name: PacketBrokerNetwork
  default: {}
- name: forwarder_enabled
  comment: Whether the server is configured as Forwarder (with gateways).
  type: bool
  default: false
- name: home_network_enabled
  comment: Whether the server is configured as Home Network (with end devices).
  type: bool
  default: false
