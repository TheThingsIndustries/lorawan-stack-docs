name: PacketBrokerRoutingPolicyDownlink
fields:
- name: join_accept
  comment: Allow join-accept messages.
  type: bool
  default: false
- name: mac_data
  comment: Allow downlink messages with FPort of 0.
  type: bool
  default: false
- name: application_data
  comment: Allow downlink messages with FPort between 1 and 255.
  type: bool
  default: false
