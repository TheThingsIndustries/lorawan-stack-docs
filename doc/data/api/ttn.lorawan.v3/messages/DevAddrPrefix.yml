name: DevAddrPrefix
fields:
- name: dev_addr
  comment: DevAddr base.
  type: bytes
  rules:
    len: 4
    ignore_empty: true
  default: ""
- name: length
  comment: Number of most significant bits from dev_addr that are used as prefix.
  type: uint32
  default: 0
