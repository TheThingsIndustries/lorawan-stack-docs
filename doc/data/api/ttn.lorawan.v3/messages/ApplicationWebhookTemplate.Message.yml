name: ApplicationWebhookTemplate.Message
fields:
- name: path
  comment: Path to append to the base URL. Can contain template fields, in RFC 6570
    format.
  type: string
  rules:
    max_len: 64
  default: ""
