name: EndDeviceTemplateFormats
fields:
- name: formats
  map_key:
    type: string
    rules:
      max_len: 36
      pattern: ^[a-z0-9](?:[-]?[a-z0-9]){2,}$
  map_value:
    message:
      name: EndDeviceTemplateFormat
  default: {}
