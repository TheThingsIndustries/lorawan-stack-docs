name: EmailNotificationPreferences
comment: EmailNotificationPreferences is the message that defines the types of notifications
  for which the user wants to receive an email.
fields:
- name: types
  rules:
    unique: true
  repeated:
    enum:
      name: NotificationType
    rules:
      defined_only: true
  default: []
