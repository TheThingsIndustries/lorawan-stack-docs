name: DecodedMessagePayload
fields:
- name: data
  message:
    package: google.protobuf
    name: Struct
  default: {}
- name: warnings
  rules:
    max_items: 10
  repeated:
    type: string
    rules:
      max_len: 100
  default: []
- name: errors
  rules:
    max_items: 10
  repeated:
    type: string
    rules:
      max_len: 100
  default: []
