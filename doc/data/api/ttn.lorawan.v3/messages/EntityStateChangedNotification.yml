name: EntityStateChangedNotification
fields:
- name: state
  enum:
    name: State
  rules:
    defined_only: true
  default: STATE_REQUESTED
- name: state_description
  type: string
  rules:
    max_len: 128
  default: ""
