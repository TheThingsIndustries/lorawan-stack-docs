name: BatchUpdateEndDeviceLastSeenRequest
fields:
- name: updates
  comment: The last seen timestamp needs to be set per end device.
  repeated:
    message:
      name: BatchUpdateEndDeviceLastSeenRequest.EndDeviceLastSeenUpdate
  default: []
