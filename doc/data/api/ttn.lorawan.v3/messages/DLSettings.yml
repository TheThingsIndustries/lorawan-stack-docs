name: DLSettings
fields:
- name: rx1_dr_offset
  enum:
    name: DataRateOffset
  rules:
    defined_only: true
  default: DATA_RATE_OFFSET_0
- name: rx2_dr
  enum:
    name: DataRateIndex
  rules:
    defined_only: true
  default: DATA_RATE_0
- name: opt_neg
  comment: OptNeg is set if Network Server implements LoRaWAN 1.1 or greater.
  type: bool
  default: false
