name: Secret
comment: Secret contains a secret value. It also contains the ID of the Encryption
  key used to encrypt it.
fields:
- name: key_id
  comment: ID of the Key used to encrypt the secret.
  type: string
  default: ""
- name: value
  type: bytes
  rules:
    max_len: 2048
  default: ""
