name: EntityAccess
methods:
  AuthInfo:
    name: AuthInfo
    comment: AuthInfo returns information about the authentication that is used on
      the request.
    input:
      package: google.protobuf
      name: Empty
    output:
      name: AuthInfoResponse
    http:
    - method: GET
      path: /auth_info
