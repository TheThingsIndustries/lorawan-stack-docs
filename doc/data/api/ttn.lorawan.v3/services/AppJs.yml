name: AppJs
comment: The AppJs service connects an Application to a Join Server.
methods:
  GetAppSKey:
    name: GetAppSKey
    comment: Request the application session key for a particular session.
    input:
      name: SessionKeyRequest
    output:
      name: AppSKeyResponse
