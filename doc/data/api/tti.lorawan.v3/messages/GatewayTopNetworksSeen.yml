name: GatewayTopNetworksSeen
comment: Networks seen by a gateway.
fields:
- name: networks
  comment: Proportion of network by name. The proportions are a fraction of 1. The
    remaining number is the rest total.
  map_key:
    type: string
  map_value:
    type: float
  default: {}
