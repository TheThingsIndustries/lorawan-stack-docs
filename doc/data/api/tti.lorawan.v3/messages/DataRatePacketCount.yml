name: DataRatePacketCount
fields:
- name: time
  message:
    package: google.protobuf
    name: Timestamp
  rules:
    required: true
  default: "0001-01-01T00:00:00Z"
- name: count
  comment: Packet counts per data rate.
  map_key:
    type: string
  map_value:
    type: uint64
  default: {}
